bin/
!bin/.gitkeep
/knowledge-mcp
*.bin
.knowledge-events.jsonl
.DS_Store
.mcp.json
.claude/.mcp.json
.claude/settings.json
.envrc

# Embedded shared assets — populated at build time by
# scripts/sync-assets.sh from .claude/agents and .claude/skills. This one
# embed feeds BOTH install-claude-assets and install-codex-assets.
# Source of truth lives at the repo root; these are build-time copies.
internal/assets/agents/
internal/assets/skills/
internal/assets/KNOWLEDGE_TOOLS.md
