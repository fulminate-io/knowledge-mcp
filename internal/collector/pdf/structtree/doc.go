// Package structtree owns the Element type and (filled by T6) the
// tagged-PDF structure-tree reader. Depends on internal/pdfcpu; consumed by
// chunk when a tagged PDF is preferred over geometric layout.
package structtree
