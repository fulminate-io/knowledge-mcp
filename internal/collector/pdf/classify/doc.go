// Package classify owns ClassifyParams and (filled by T5) the heading /
// list / caption classifier that assigns BlockKind values to layout
// blocks. Imports regexp; consumed by chunk.
package classify
