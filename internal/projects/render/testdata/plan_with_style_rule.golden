# Plan: p-with-style

p-with-style (plan) [active]
  ID: <ID>

## Language patterns

- <ID> — severity=warning — Functions do not use naked returns.
- <ID> — lang-pattern-fixture

```
defer $DB.Close()
```

**Style rules are REFERENCES, not hydrated.** Read them in ONE bulk call — `query(graph:"practice", ids:["<ID>"])` — never one call per rule, which is what hits the tool's response limits.
415 rendered bytes.