# Ticket: t-uses-edge

**Status:** open

uses desc

ID: <ID>

## Patterns

- <ID> — severity=warning — Loops over a map are sorted by key before iteration, neve...
- [active] lang-pattern-fixture — ID: <ID>

## Language patterns

*No language patterns attached.*

**Style rules are REFERENCES, not hydrated.** Read them in ONE bulk call — `query(graph:"practice", ids:["<ID>"])` — never one call per rule, which is what hits the tool's response limits.
476 rendered bytes.